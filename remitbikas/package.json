{
  "name": "remitbikas",
  "version": "1.0.0",
  "description": "RemitBikas - Municipal Project Transparency Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "db:push": "npm run db:push --prefix backend",
    "db:seed": "npm run prisma:seed --prefix backend",
    "db:setup": "npm run db:push && npm run db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
